# VLM-MPPI core dependencies
# All licenses: Apache 2.0 or BSD — safe for commercial use

# PyTorch (BSD-3) — install separately if you need a specific CUDA version:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# HuggingFace ecosystem (Apache 2.0)
transformers>=4.45.0
accelerate>=0.26.0
qwen-vl-utils>=0.0.8

# Image / video handling
Pillow>=10.0.0
opencv-python>=4.8.0

# Numerical
numpy>=1.24.0

# Optional: camera interface
# pyrealsense2>=2.54.0   # uncomment if using Intel RealSense

# Optional: for 4-bit quantization on smaller GPUs
# auto-gptq>=0.7.0
# autoawq>=0.2.0
